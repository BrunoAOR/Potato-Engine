TO DO list:

* Modify the Renderer so the SDL_Texture instances are managed by the RenderersManager to avoid duplications.

* Review all engine classes and implement (or delete) copy constructors/assignments and move constructors/assignments
